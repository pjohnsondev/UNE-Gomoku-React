export enum PLAYER {
    NONE = "NONE",
    WHITE = "WHITE",
    BLACK = "BLACK"
}

export enum TileSelectionType {
    SELECT = "SELECT",
    CLEAR = "CLEAR"
}

export enum GAMESTATUS {
    ACTIVE = "ACTIVE",
    COMPLETE = "COMPLETE",
    DRAW = "DRAW"
}